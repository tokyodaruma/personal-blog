<h1>Hello, old friend!</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
      <%= "#{article.body.split(' ').first(3).join(' ')}..." %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
